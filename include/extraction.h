#ifndef EXTRACTION_H
#define EXTRACTION_H

#include <iostream>
#include <cstring>
#include <string>

using namespace std;

bool contains(char *, const char *, char);
string extraction(const char *);

#endif